body,
body * {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: "Roboto";
}

body {
  display: flex;
  min-height: 100vh;
}

body > div {
  padding: 20px;
}
div.left {
  flex: 2;
}
div.right {
  flex: 8;
  background-color: rgba(0, 0, 0, 0.8);
  position: relative;
}
div.note {
  background-color: #989898;
  width: 300px;
  margin-bottom: 20px;
  cursor: move;
  box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.6);
  position: absolute;
  z-index: 99;
}
div.note h3 {
  background-color: #787878;
  padding: 5px 10px;
  font-size: 15px;
  color: #fff;
  border: 1px solid #000;
}
div.note p {
  font-size: 13px;
  color: #fff;
  padding: 10px;
  border: 1px solid #000;
  line-height: 1.3;
}
div.note .close_note {
  position: absolute;
  right: 10px;
  top: 5px;
  cursor: pointer;
}

.highlight{
  background-color: yellow;
}
.textClass::-moz-selection { /* Code for Firefox */
  background: yellow;
}
.textClass::selection {
  background: yellow;
}
